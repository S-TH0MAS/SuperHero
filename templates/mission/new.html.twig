{% extends 'base.html.twig' %}

{% block title %}Nouvelle Mission - {{ parent() }}{% endblock %}

{% block body %}
    {{ include('components/_page_header.html.twig', {
        'title': 'Créer une nouvelle Mission',
        'subtitle': 'Planifiez une nouvelle mission pour vos équipes',
        'icon': 'fas fa-clipboard-list',
        'actions': [{'url': path('app_mission_index'), 'label': 'Retour à la liste', 'icon': 'fas fa-arrow-left', 'class': 'btn-light'}]
    }) }}

    <div class="card">
        <div class="card-body">
            {{ include('mission/_form.html.twig', {'button_label': 'Créer la mission'}) }}
        </div>
    </div>
{% endblock %}
